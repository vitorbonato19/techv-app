spring:
  jackson: America/Sao_Paulo
  datasource:
    url : jdbc:mysql://localhost:3306/CENTRALTECHV
    username: ADMIN
    password: ADMIN
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  sql:
    init:
      schema-locations: classpath:data.sql